<template>
  <div class="container">
    <div class="reverse mt-5">
      <h3 style="text-transform: uppercase" class="reverse__title">{{$t('reservation.title')}}</h3>
      <p class="reverse__text pt-4 pb-4">{{$t('reservation.text')}}</p>
      <button class="reverse-btn">{{$t('reservation.title')}}</button>
    </div>
  </div>
</template>